{
  "name": "gk-cab",
  "version": "1.0.0",
  "description": "GK Cab Application",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && npm install && npm run build",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": "16.x"
  }
}
